# Storybook Package

This package is a shared Storybook config shell for the monorepo.

When you add a dedicated Storybook app later, it can import:

- `@awesomeui/storybook/main`
- `@awesomeui/storybook/preview`

